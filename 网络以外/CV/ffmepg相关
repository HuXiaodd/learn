编解码器特性:
| 特性 | H.264 (AVC) | H.265 (HEVC) |
| :--- | :--- | :--- |
| 基本单元 | 宏块 (16x16) | 编码树单元 (CTU, 最大64x64) |
| 划分方式 | 固定子块划分 | 四叉树递归划分 |
| 帧内预测 | 9种模式 | 35种模式 |
| 压缩效率 | 标准 | 比H.264高约50% |
| 计算复杂度 | 较低，兼容性极好 | 较高，对硬件要求更高 |
帧类型:
视频序列并非简单地一帧帧独立处理，而是被组织成图像组（GOP, Group of Pictures），其中包含三种基本帧类型：
I帧 (Intra-coded frame): 帧内编码帧。它不依赖任何其他帧，可以独立解码，相当于一个完整的静态图片。I帧是GOP的起点，用于随机访问和错误恢复，但压缩率最低。
P帧 (Predictive-coded frame): 前向预测编码帧。它参考前面的I帧或P帧，通过运动估计来预测当前帧内容，只编码差异部分。压缩率中等。
B帧 (Bi-directional predictive-coded frame): 双向预测编码帧。它同时参考前后两帧（I帧或P帧）进行预测，因此压缩率最高，但会增加编解码的延迟

p预测帧采用残差,即为两帧做差,得到的差为一帧,这种帧借助前图像,和前帧做异或(相同图为残差轨迹)即得到当前帧,同时,编解码中解码顺序应该
是i--p--b的顺序解码,不用按照实际的帧顺序解码/编码，同时b帧不能依赖b帧去生成图像,b帧也同样不能被p帧依赖.b帧是解码的终点.
多个连续b帧之间的(比如依赖两个i帧)预测位置是时间线性的,所以连续b帧的位置不相同.

quest:如果存在两个gop,是连续gop,第一个gop第一帧是i帧其他帧全是b帧,第二个gop全是b帧,它对内存的影响就非常大了
answer:IDR帧
你担心的“连续 GOP 导致内存溢出”，正是 IDR 帧 存在的意义。
普通 I 帧：比较温和，允许后面的 B 帧跨过去参考它（开放式 GOP）。这确实会增加内存占用，通常用于极高压缩率的场景（如蓝光原盘），但解码器必须有大缓存。
IDR 帧：非常霸道，强制切断所有跨 GOP 的参考。这是直播、视频会议、在线流媒体的标准配置。它保证了：
内存可控：解码器永远只需要存当前 GOP 的几张图。
错误不扩散：如果 GOP1 的数据坏了，遇到 IDR 帧，错误就被“一刀两断”，不会影响到 GOP2

